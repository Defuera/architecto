name: architecto
description: Architecture playground
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  riverpod_sample:
    path: ./riverpod_sample

  bloc_sample:
    path: ./bloc_sample

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  dart_code_metrics: ^4.9.1

flutter:
  uses-material-design: true